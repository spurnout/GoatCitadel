# Perf Snapshot Summary (2026-03-05)

## Sources
- Before: `artifacts/prompt-lab/gate-run-20260305-040958Z.md`
- After: `artifacts/prompt-lab/gate-run-20260305-062942Z.md`
- Coverage: `artifacts/coverage/coverage-summary.json`

## Prompt Gate Delta
- Average score: `8.51 -> 8.60` (`+0.09`)
- Pass rate @ `7/10`: `87.9% -> 89.6%` (`+1.7 pts`)
- Run failures: `0 -> 0`
- Score failures: `0 -> 0`

## Optimization Status
- Chat tool loop now emits `trace_update` progress during loops.
- Final assistant generation can stream `delta` output in the loop path.
- Intent detectors are computed once per request and reused.
- Cron single-job checks use direct `cronJobs.get(jobId)` lookups.
- SSE token validation moved from linear scan to keyed lookup.
- Dashboard now subscribes to refresh topic `dashboard`.
- Checkpoint listing supports bounded pagination.
- Dead-letter upsert path avoids list-and-find follow-up scan.
- TUI poll fallback deduplicates already-seen events.
- App-wide 1-second clock rerender removed from root component.
- SQLite tuning knobs added with safe bounded config defaults.

## Notes
- This snapshot is intended as a cycle checkpoint and release artifact.
- Dedicated synthetic micro-benchmarks for chat TTFB and SSE validation fanout can be added in the next perf pass.
